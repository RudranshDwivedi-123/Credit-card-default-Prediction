# Credit-card-default-Prediction
Machine learning project predicting whether a credit card client will default on payment next month, using the UCI "Default of Credit Card Clients" dataset. Built with Logistic Regression and Random Forest in Python (pandas, scikit-learn, seaborn).
